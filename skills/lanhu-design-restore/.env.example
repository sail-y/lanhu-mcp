# 蓝湖 skill 本地配置文件
#
# 复制本文件为同目录下的 .env 后填入真实值即可：
#   cp .env.example .env
#
# 本文件由 skill 配置模块 (lanhu/tools/config.py) 读取，作为「环境变量缺失时的兜底」。
# 优先级：环境变量 > 本文件。
# 注意：.env 含 Cookie 等敏感信息，已被 .gitignore 忽略，请勿提交。

# 蓝湖登录 Cookie（必填）。从浏览器开发者工具复制 lanhuapp.com 的请求 Cookie。
LANHU_COOKIE=
